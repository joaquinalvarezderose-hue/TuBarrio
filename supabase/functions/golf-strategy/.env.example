# Variables para levantar esta funcion localmente con:
#   supabase functions serve golf-strategy --env-file supabase/functions/golf-strategy/.env.local
#
# Copiar este archivo a .env.local (gitignored) y completar los valores.
# SUPABASE_URL y SUPABASE_ANON_KEY son publicos (no son secretos), se
# pueden tomar del proyecto ya linkeado. ANTHROPIC_API_KEY si es secreto:
# nunca commitear un valor real.

ANTHROPIC_API_KEY=sk-ant-...
SUPABASE_URL=https://bpgyqjfysapldrlnsoty.supabase.co
SUPABASE_ANON_KEY=...
